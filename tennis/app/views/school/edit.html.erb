<% provide(:title, "Aanspassen") %>
<div id="inhoud">
  	<h1>aanpassen spelers</h1>
    <div class="formParagraph">
	  	<%= form_for(@school) do |f| %>
		    <%= render 'error_messages' %>
                <p><%= f.label :naam %></p>
                <%= f.text_field :naam, class: 'form-control' %>
		        <br/><br/>
		      <%= f.submit "Opslaan", class: "submit" %>
    	<% end %>
  	</div>
</div>
<div id="inhoudBreak">
</div>